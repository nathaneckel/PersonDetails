package com.zipcodewilmington;
/**
 * Created by leon on 1/24/18.
 */
public class PersonHandler {
    private final Person[] personArray;
    public PersonHandler(Person[] personArray) {
        this.personArray = personArray;
    }

    // NOTICE how the comments can step-by-step break the problem into simple lines of code...
    public static void main(String[] args) {
        // create a `counter`
        int counter = 0;
        // while `counter` is less than length of array
        int counter = personArray.length;
        while (boolean counter < personArray.length; counter++) {

        }
            // begin loop
                // use `counter` to identify the `current Person` in the array

        for (int i = 0; i < currentPerson; personArray[counter];
                // get `string Representation` of `currentPerson`
        currentPerson[]
                // append `stringRepresentation` to `result` variable
                return new StringBuilder()
                        .append(stringRepresentation.result)
                        .toString()
            // end loop
        return result;
    }



    public String forLoop() {
        String result = "";
        // identify initial value
        // identify terminal condition
        // identify increment

        // use the above clauses to declare for-loop signature
            // begin loop
                // use `counter` to identify the `current Person` in the array
                // get `string Representation` of `currentPerson`
                // append `stringRepresentation` to `result` variable
            // end loop

        return result;
    }



    public static void String (forEachLoop[]
            {
        // identify array's type
        // identify array's variable-name
        String result = "";

        // use the above discoveries to declare for-each-loop signature
        for <result[]
            // begin loop
                // get `string Representation` of `currentPerson`
                // append `stringRepresentation` to `result` variable
            // end loop

        return result;
    }


    public Person[] getPersonArray() {
        return personArray;
    }
}
